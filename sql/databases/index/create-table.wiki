= CREATE TABLE =
Creates a new table in a database
{{{
	CREATE TABLE table_name (
		column1 datatype,
		column2 datatype,
		column3 datatype,
	);
}}}

Column parameters specify the name of the fields(columns) of the table.

The datatype parameter specifies the [[datatype|type of data]] the column
