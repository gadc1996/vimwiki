= DELETE =
The DELETE command is used to erase existing records in a table.
`DELETE FROM custumers WHERE name='Alfred'`
`DELETE FROM custumers`

Be carefull when deleting, if no WHERE condition is specified, all the data
in the table will be erased.
