= IN =
The in command alow to specify multiple values in a where clause.
`SELECT * FROM customers WHERE country IN ( 'Germany', 'France', 'UK' );`
`SELECT * FROM customers WHERE country NOT IN ( 'Germany', 'France', 'UK' );`
